
import ChatBubbles from "../components/ChatChanel/ChatChanel";

const Chat = () => {
  // ค่าเริ่มต้นเป็น "venter"

  return (
    <div className="text-center p-10">
      <ChatBubbles  />
    </div>
  );
};

export default Chat;
